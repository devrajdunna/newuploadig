<%= form_for(crkpdatum) do |f| %>
  <% if crkpdatum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crkpdatum.errors.count, "error") %> prohibited this crkpdatum from being saved:</h2>

      <ul>
      <% crkpdatum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :adhar %>
    <%= f.text_field :adhar %>
  </div>

  <div class="field">
    <%= f.label :mobile %>
    <%= f.text_field :mobile %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
